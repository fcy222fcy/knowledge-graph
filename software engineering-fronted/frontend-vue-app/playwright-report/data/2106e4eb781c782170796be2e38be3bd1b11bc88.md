# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: register.spec.ts >> 注册页面测试 >> 密码确认验证
- Location: tests\register.spec.ts:54:3

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: locator.fill: Test timeout of 30000ms exceeded.
Call log:
  - waiting for locator('input[placeholder="请输入昵称"]')

```

# Page snapshot

```yaml
- generic [ref=e4]:
  - generic [ref=e5]:
    - generic [ref=e6]: SE
    - heading "创建账号" [level=1] [ref=e7]
    - paragraph [ref=e8]: 加入 SE智图问答，开启智能学习
  - generic [ref=e9]:
    - generic [ref=e13]:
      - img [ref=e16]
      - textbox "请输入用户名" [active] [ref=e18]: testuser
    - generic [ref=e22]:
      - img [ref=e25]
      - textbox "请输入邮箱" [ref=e28]
    - generic [ref=e32]:
      - img [ref=e35]
      - textbox "请输入密码" [ref=e38]
    - generic [ref=e42]:
      - img [ref=e45]
      - textbox "请再次输入密码" [ref=e48]
    - button "注册" [ref=e51] [cursor=pointer]:
      - generic [ref=e52]: 注册
  - generic [ref=e53]:
    - text: 已有账号？
    - generic [ref=e55] [cursor=pointer]: 立即登录
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test'
  2  | 
  3  | test.describe('注册页面测试', () => {
  4  |   test.beforeEach(async ({ page }) => {
  5  |     await page.goto('/register')
  6  |   })
  7  | 
  8  |   test('页面加载正确', async ({ page }) => {
  9  |     // 检查页面标题
  10 |     await expect(page).toHaveTitle(/SE智图问答/)
  11 | 
  12 |     // 检查注册表单存在
  13 |     await expect(page.locator('.register-card')).toBeVisible()
  14 |   })
  15 | 
  16 |   test('表单元素完整', async ({ page }) => {
  17 |     // 检查用户名输入框
  18 |     const usernameInput = page.locator('input[placeholder="请输入用户名"]')
  19 |     await expect(usernameInput).toBeVisible()
  20 | 
  21 |     // 检查昵称输入框
  22 |     const nicknameInput = page.locator('input[placeholder="请输入昵称"]')
  23 |     await expect(nicknameInput).toBeVisible()
  24 | 
  25 |     // 检查密码输入框
  26 |     const passwordInput = page.locator('input[placeholder="请输入密码"]')
  27 |     await expect(passwordInput).toBeVisible()
  28 | 
  29 |     // 检查确认密码输入框
  30 |     const confirmPasswordInput = page.locator('input[placeholder="请再次输入密码"]')
  31 |     await expect(confirmPasswordInput).toBeVisible()
  32 | 
  33 |     // 检查注册按钮
  34 |     const registerButton = page.locator('button:has-text("注册")')
  35 |     await expect(registerButton).toBeVisible()
  36 | 
  37 |     // 检查登录链接
  38 |     await expect(page.locator('text=已有账号？')).toBeVisible()
  39 |     await expect(page.locator('text=立即登录')).toBeVisible()
  40 |   })
  41 | 
  42 |   test('表单验证 - 空值提交', async ({ page }) => {
  43 |     // 点击注册按钮 without filling form
  44 |     const registerButton = page.locator('button:has-text("注册")')
  45 |     await registerButton.click()
  46 | 
  47 |     // 检查验证错误消息
  48 |     await expect(page.locator('text=请输入用户名')).toBeVisible()
  49 |     await expect(page.locator('text=请输入昵称')).toBeVisible()
  50 |     await expect(page.locator('text=请输入密码')).toBeVisible()
  51 |     await expect(page.locator('text=请再次输入密码')).toBeVisible()
  52 |   })
  53 | 
  54 |   test('密码确认验证', async ({ page }) => {
  55 |     // 填写表单
  56 |     const usernameInput = page.locator('input[placeholder="请输入用户名"]')
  57 |     await usernameInput.fill('testuser')
  58 | 
  59 |     const nicknameInput = page.locator('input[placeholder="请输入昵称"]')
> 60 |     await nicknameInput.fill('测试用户')
     |                         ^ Error: locator.fill: Test timeout of 30000ms exceeded.
  61 | 
  62 |     const passwordInput = page.locator('input[placeholder="请输入密码"]')
  63 |     await passwordInput.fill('password123')
  64 | 
  65 |     const confirmPasswordInput = page.locator('input[placeholder="请再次输入密码"]')
  66 |     await confirmPasswordInput.fill('password456') // 不匹配的密码
  67 | 
  68 |     // 点击注册按钮
  69 |     const registerButton = page.locator('button:has-text("注册")')
  70 |     await registerButton.click()
  71 | 
  72 |     // 检查密码不匹配错误
  73 |     await expect(page.locator('text=两次输入的密码不一致')).toBeVisible()
  74 |   })
  75 | 
  76 |   test('导航到登录页面', async ({ page }) => {
  77 |     // 点击登录链接
  78 |     const loginLink = page.locator('text=立即登录')
  79 |     await loginLink.click()
  80 | 
  81 |     // 验证导航到登录页面
  82 |     await expect(page).toHaveURL('/login')
  83 |   })
  84 | 
  85 |   test('页面响应式布局', async ({ page }) => {
  86 |     // 测试桌面视图
  87 |     await page.setViewportSize({ width: 1200, height: 800 })
  88 |     await expect(page.locator('.register-card')).toBeVisible()
  89 | 
  90 |     // 测试移动端视图
  91 |     await page.setViewportSize({ width: 375, height: 667 })
  92 |     await expect(page.locator('.register-card')).toBeVisible()
  93 |   })
  94 | })
  95 | 
```