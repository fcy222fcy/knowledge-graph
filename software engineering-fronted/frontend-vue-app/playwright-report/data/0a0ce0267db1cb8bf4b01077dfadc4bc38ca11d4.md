# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: knowledge-graph.spec.ts >> 知识图谱页面测试 >> 页面加载正确
- Location: tests\knowledge-graph.spec.ts:18:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('.main')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('.main')

```

```yaml
- text: SE
- heading "欢迎回来" [level=1]
- paragraph: 登录 SE智图问答，继续你的学习之旅
- img
- textbox "请输入用户名"
- img
- textbox "请输入密码"
- checkbox "记住我" [checked]
- text: 记住我 忘记密码？
- button "登录"
- text: 还没有账号？ 立即注册
```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test'
  2   | 
  3   | test.describe('知识图谱页面测试', () => {
  4   |   test.beforeEach(async ({ page }) => {
  5   |     // 设置 localStorage 模拟已登录状态
  6   |     await page.goto('/login')
  7   |     await page.evaluate(() => {
  8   |       localStorage.setItem('token', 'test-token')
  9   |       localStorage.setItem('userInfo', JSON.stringify({
  10  |         id: 1,
  11  |         username: 'testuser',
  12  |         nickname: '测试用户'
  13  |       }))
  14  |     })
  15  |     await page.goto('/knowledge-graph')
  16  |   })
  17  | 
  18  |   test('页面加载正确', async ({ page }) => {
  19  |     // 检查页面标题
  20  |     await expect(page).toHaveTitle(/SE智图问答/)
  21  | 
  22  |     // 检查主布局存在
  23  |     await expect(page.locator('.app-layout')).toBeVisible()
  24  |     await expect(page.locator('.sidebar')).toBeVisible()
> 25  |     await expect(page.locator('.main')).toBeVisible()
      |                                         ^ Error: expect(locator).toBeVisible() failed
  26  |   })
  27  | 
  28  |   test('图谱工具栏完整', async ({ page }) => {
  29  |     // 等待页面加载
  30  |     await page.waitForLoadState('networkidle')
  31  | 
  32  |     // 检查工具栏存在
  33  |     await expect(page.locator('.graph-toolbar')).toBeVisible()
  34  | 
  35  |     // 检查构建图谱按钮
  36  |     await expect(page.locator('button:has-text("构建图谱")')).toBeVisible()
  37  | 
  38  |     // 检查搜索框
  39  |     await expect(page.locator('input[placeholder="搜索节点..."]')).toBeVisible()
  40  |   })
  41  | 
  42  |   test('图谱画布存在', async ({ page }) => {
  43  |     // 等待页面加载
  44  |     await page.waitForLoadState('networkidle')
  45  | 
  46  |     // 检查图谱画布存在
  47  |     await expect(page.locator('.graph-canvas')).toBeVisible()
  48  | 
  49  |     // 检查SVG元素（D3.js图谱）
  50  |     await expect(page.locator('svg')).toBeVisible()
  51  |   })
  52  | 
  53  |   test('节点详情面板', async ({ page }) => {
  54  |     // 等待页面加载
  55  |     await page.waitForLoadState('networkidle')
  56  | 
  57  |     // 检查节点详情面板存在
  58  |     await expect(page.locator('.node-detail')).toBeVisible()
  59  |   })
  60  | 
  61  |   test('构建图谱对话框', async ({ page }) => {
  62  |     // 等待页面加载
  63  |     await page.waitForLoadState('networkidle')
  64  | 
  65  |     // 点击构建图谱按钮
  66  |     const buildButton = page.locator('button:has-text("构建图谱")')
  67  |     await buildButton.click()
  68  | 
  69  |     // 检查对话框出现
  70  |     await expect(page.locator('.el-dialog')).toBeVisible()
  71  |     await expect(page.locator('text=构建知识图谱')).toBeVisible()
  72  |   })
  73  | 
  74  |   test('图谱搜索功能', async ({ page }) => {
  75  |     // 等待页面加载
  76  |     await page.waitForLoadState('networkidle')
  77  | 
  78  |     // 找到搜索框
  79  |     const searchInput = page.locator('input[placeholder="搜索节点..."]')
  80  |     await expect(searchInput).toBeVisible()
  81  | 
  82  |     // 输入搜索内容
  83  |     await searchInput.fill('软件工程')
  84  | 
  85  |     // 检查搜索结果（如果有的话）
  86  |     // 这里需要根据实际实现来调整
  87  |   })
  88  | 
  89  |   test('图谱交互功能', async ({ page }) => {
  90  |     // 等待页面加载
  91  |     await page.waitForLoadState('networkidle')
  92  | 
  93  |     // 检查图谱缩放控制
  94  |     const zoomIn = page.locator('button:has-text("+")')
  95  |     const zoomOut = page.locator('button:has-text("-")')
  96  | 
  97  |     if (await zoomIn.isVisible()) {
  98  |       await zoomIn.click()
  99  |     }
  100 | 
  101 |     if (await zoomOut.isVisible()) {
  102 |       await zoomOut.click()
  103 |     }
  104 |   })
  105 | 
  106 |   test('页面响应式布局', async ({ page }) => {
  107 |     // 测试桌面视图
  108 |     await page.setViewportSize({ width: 1200, height: 800 })
  109 |     await expect(page.locator('.graph-canvas')).toBeVisible()
  110 | 
  111 |     // 测试移动端视图
  112 |     await page.setViewportSize({ width: 375, height: 667 })
  113 |     await expect(page.locator('.graph-canvas')).toBeVisible()
  114 |   })
  115 | })
  116 | 
```