# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: home.spec.ts >> 首页测试 >> 导航到各个页面
- Location: tests\home.spec.ts:72:3

# Error details

```
Error: locator.click: Error: strict mode violation: locator('text=知识图谱') resolved to 2 elements:
    1) <span data-v-22686b16="">知识图谱</span> aka getByRole('link', { name: '知识图谱', exact: true })
    2) <span data-v-78e13021="" class="action-title">知识图谱</span> aka getByRole('link', { name: '知识图谱 可视化知识网络' })

Call log:
  - waiting for locator('text=知识图谱')

```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - generic [ref=e4]:
    - generic [ref=e5]:
      - generic [ref=e6]: SE
      - heading "欢迎回来" [level=1] [ref=e7]
      - paragraph [ref=e8]: 登录 SE智图问答，继续你的学习之旅
    - generic [ref=e9]:
      - generic [ref=e13]:
        - img [ref=e16]
        - textbox "请输入用户名" [ref=e18]
      - generic [ref=e22]:
        - img [ref=e25]
        - textbox "请输入密码" [ref=e28]
      - generic [ref=e31]:
        - generic [ref=e32] [cursor=pointer]:
          - generic [ref=e33]:
            - checkbox "记住我" [checked]
          - generic [ref=e35]: 记住我
        - generic [ref=e37] [cursor=pointer]: 忘记密码？
      - button "登录" [ref=e40] [cursor=pointer]:
        - generic [ref=e41]: 登录
    - generic [ref=e42]:
      - text: 还没有账号？
      - generic [ref=e44] [cursor=pointer]: 立即注册
  - alert [ref=e45]:
    - img [ref=e47]
    - paragraph [ref=e49]: 登录已过期，请重新登录
```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test'
  2   | 
  3   | test.describe('首页测试', () => {
  4   |   test.beforeEach(async ({ page }) => {
  5   |     // 设置 localStorage 模拟已登录状态
  6   |     await page.goto('/login')
  7   |     await page.evaluate(() => {
  8   |       localStorage.setItem('token', 'test-token')
  9   |       localStorage.setItem('userInfo', JSON.stringify({
  10  |         id: 1,
  11  |         username: 'testuser',
  12  |         nickname: '测试用户'
  13  |       }))
  14  |     })
  15  |     await page.goto('/home')
  16  |   })
  17  | 
  18  |   test('页面加载正确', async ({ page }) => {
  19  |     // 检查页面标题
  20  |     await expect(page).toHaveTitle(/SE智图问答/)
  21  | 
  22  |     // 检查主布局存在
  23  |     await expect(page.locator('.app-layout')).toBeVisible()
  24  |     await expect(page.locator('.sidebar')).toBeVisible()
  25  |     await expect(page.locator('.main')).toBeVisible()
  26  |   })
  27  | 
  28  |   test('侧边栏导航完整', async ({ page }) => {
  29  |     // 检查Logo
  30  |     await expect(page.locator('.logo-text')).toHaveText('SE智图问答')
  31  | 
  32  |     // 检查导航项
  33  |     const navItems = page.locator('.nav-item')
  34  |     await expect(navItems).toHaveCount(5)
  35  | 
  36  |     // 检查各个导航项
  37  |     await expect(page.locator('text=首页')).toBeVisible()
  38  |     await expect(page.locator('text=知识图谱')).toBeVisible()
  39  |     await expect(page.locator('text=问答中心')).toBeVisible()
  40  |     await expect(page.locator('text=资料管理')).toBeVisible()
  41  |     await expect(page.locator('text=分析统计')).toBeVisible()
  42  |   })
  43  | 
  44  |   test('用户信息显示', async ({ page }) => {
  45  |     // 检查用户头像
  46  |     await expect(page.locator('.user-avatar')).toBeVisible()
  47  | 
  48  |     // 检查用户名
  49  |     await expect(page.locator('.user-name')).toHaveText('测试用户')
  50  | 
  51  |     // 检查用户角色
  52  |     await expect(page.locator('.user-role')).toHaveText('软件工程专业')
  53  |   })
  54  | 
  55  |   test('首页内容加载', async ({ page }) => {
  56  |     // 等待加载完成
  57  |     await page.waitForLoadState('networkidle')
  58  | 
  59  |     // 检查今日统计组件
  60  |     await expect(page.locator('.today-stats')).toBeVisible()
  61  | 
  62  |     // 检查快捷操作组件
  63  |     await expect(page.locator('.quick-actions')).toBeVisible()
  64  | 
  65  |     // 检查最近问答组件
  66  |     await expect(page.locator('.recent-qa-list')).toBeVisible()
  67  | 
  68  |     // 检查学习趋势组件
  69  |     await expect(page.locator('.learning-trend')).toBeVisible()
  70  |   })
  71  | 
  72  |   test('导航到各个页面', async ({ page }) => {
  73  |     // 导航到知识图谱
> 74  |     await page.locator('text=知识图谱').click()
      |                                     ^ Error: locator.click: Error: strict mode violation: locator('text=知识图谱') resolved to 2 elements:
  75  |     await expect(page).toHaveURL('/knowledge-graph')
  76  | 
  77  |     // 导航到问答中心
  78  |     await page.locator('text=问答中心').click()
  79  |     await expect(page).toHaveURL('/qa')
  80  | 
  81  |     // 导航到资料管理
  82  |     await page.locator('text=资料管理').click()
  83  |     await expect(page).toHaveURL('/files')
  84  | 
  85  |     // 导航到分析统计
  86  |     await page.locator('text=分析统计').click()
  87  |     await expect(page).toHaveURL('/stats')
  88  | 
  89  |     // 导航回首页
  90  |     await page.locator('text=首页').click()
  91  |     await expect(page).toHaveURL('/home')
  92  |   })
  93  | 
  94  |   test('退出登录功能', async ({ page }) => {
  95  |     // 点击用户卡片
  96  |     const userCard = page.locator('.user-card')
  97  |     await userCard.click()
  98  | 
  99  |     // 检查确认对话框
  100 |     await expect(page.locator('.el-message-box')).toBeVisible()
  101 |     await expect(page.locator('text=确定要退出登录吗？')).toBeVisible()
  102 | 
  103 |     // 点击确定按钮
  104 |     await page.locator('button:has-text("确定")').click()
  105 | 
  106 |     // 验证导航到登录页面
  107 |     await expect(page).toHaveURL('/login')
  108 |   })
  109 | 
  110 |   test('页面响应式布局', async ({ page }) => {
  111 |     // 测试桌面视图
  112 |     await page.setViewportSize({ width: 1200, height: 800 })
  113 |     await expect(page.locator('.sidebar')).toBeVisible()
  114 |     await expect(page.locator('.main')).toBeVisible()
  115 | 
  116 |     // 测试移动端视图
  117 |     await page.setViewportSize({ width: 375, height: 667 })
  118 |     // 侧边栏可能隐藏或变为抽屉式
  119 |     await expect(page.locator('.main')).toBeVisible()
  120 |   })
  121 | })
  122 | 
```