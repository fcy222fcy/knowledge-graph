# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: files.spec.ts >> 资料管理页面测试 >> 文档表格存在
- Location: tests\files.spec.ts:28:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('.document-table')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('.document-table')

```

```yaml
- text: SE
- heading "欢迎回来" [level=1]
- paragraph: 登录 SE智图问答，继续你的学习之旅
- img
- textbox "请输入用户名"
- img
- textbox "请输入密码"
- checkbox "记住我" [checked]
- text: 记住我 忘记密码？
- button "登录"
- text: 还没有账号？ 立即注册
```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test'
  2   | 
  3   | test.describe('资料管理页面测试', () => {
  4   |   test.beforeEach(async ({ page }) => {
  5   |     // 设置 localStorage 模拟已登录状态
  6   |     await page.goto('/login')
  7   |     await page.evaluate(() => {
  8   |       localStorage.setItem('token', 'test-token')
  9   |       localStorage.setItem('userInfo', JSON.stringify({
  10  |         id: 1,
  11  |         username: 'testuser',
  12  |         nickname: '测试用户'
  13  |       }))
  14  |     })
  15  |     await page.goto('/files')
  16  |   })
  17  | 
  18  |   test('页面加载正确', async ({ page }) => {
  19  |     // 检查页面标题
  20  |     await expect(page).toHaveTitle(/SE智图问答/)
  21  | 
  22  |     // 检查主布局存在
  23  |     await expect(page.locator('.app-layout')).toBeVisible()
  24  |     await expect(page.locator('.sidebar')).toBeVisible()
  25  |     await expect(page.locator('.main')).toBeVisible()
  26  |   })
  27  | 
  28  |   test('文档表格存在', async ({ page }) => {
  29  |     // 等待页面加载
  30  |     await page.waitForLoadState('networkidle')
  31  | 
  32  |     // 检查文档表格存在
> 33  |     await expect(page.locator('.document-table')).toBeVisible()
      |                                                   ^ Error: expect(locator).toBeVisible() failed
  34  | 
  35  |     // 检查表格标题
  36  |     await expect(page.locator('text=文档名称')).toBeVisible()
  37  |     await expect(page.locator('text=文档类型')).toBeVisible()
  38  |     await expect(page.locator('text=上传时间')).toBeVisible()
  39  |     await expect(page.locator('text=操作')).toBeVisible()
  40  |   })
  41  | 
  42  |   test('上传按钮存在', async ({ page }) => {
  43  |     // 等待页面加载
  44  |     await page.waitForLoadState('networkidle')
  45  | 
  46  |     // 检查上传按钮
  47  |     await expect(page.locator('button:has-text("上传文档")')).toBeVisible()
  48  |   })
  49  | 
  50  |   test('上传对话框功能', async ({ page }) => {
  51  |     // 等待页面加载
  52  |     await page.waitForLoadState('networkidle')
  53  | 
  54  |     // 点击上传按钮
  55  |     const uploadButton = page.locator('button:has-text("上传文档")')
  56  |     await uploadButton.click()
  57  | 
  58  |     // 检查上传对话框出现
  59  |     await expect(page.locator('.el-dialog')).toBeVisible()
  60  |     await expect(page.locator('text=上传文档')).toBeVisible()
  61  | 
  62  |     // 检查文件上传区域
  63  |     await expect(page.locator('.el-upload')).toBeVisible()
  64  |   })
  65  | 
  66  |   test('文档详情功能', async ({ page }) => {
  67  |     // 等待页面加载
  68  |     await page.waitForLoadState('networkidle')
  69  | 
  70  |     // 检查文档详情组件
  71  |     const documentDetail = page.locator('.document-detail')
  72  |     if (await documentDetail.isVisible()) {
  73  |       // 检查详情内容
  74  |       await expect(page.locator('text=文档信息')).toBeVisible()
  75  |     }
  76  |   })
  77  | 
  78  |   test('文档操作按钮', async ({ page }) => {
  79  |     // 等待页面加载
  80  |     await page.waitForLoadState('networkidle')
  81  | 
  82  |     // 检查操作按钮
  83  |     const actionButtons = page.locator('.action-btn')
  84  |     const count = await actionButtons.count()
  85  | 
  86  |     if (count > 0) {
  87  |       // 检查第一个文档的操作按钮
  88  |       await expect(actionButtons.first()).toBeVisible()
  89  |     }
  90  |   })
  91  | 
  92  |   test('分页功能', async ({ page }) => {
  93  |     // 等待页面加载
  94  |     await page.waitForLoadState('networkidle')
  95  | 
  96  |     // 检查分页组件
  97  |     const pagination = page.locator('.el-pagination')
  98  |     if (await pagination.isVisible()) {
  99  |       // 检查分页信息
  100 |       await expect(page.locator('text=共')).toBeVisible()
  101 |     }
  102 |   })
  103 | 
  104 |   test('搜索功能', async ({ page }) => {
  105 |     // 等待页面加载
  106 |     await page.waitForLoadState('networkidle')
  107 | 
  108 |     // 检查搜索框
  109 |     const searchInput = page.locator('input[placeholder*="搜索"]')
  110 |     if (await searchInput.isVisible()) {
  111 |       // 输入搜索内容
  112 |       await searchInput.fill('软件工程')
  113 | 
  114 |       // 检查搜索结果
  115 |       // 这里需要根据实际实现来调整
  116 |     }
  117 |   })
  118 | 
  119 |   test('页面响应式布局', async ({ page }) => {
  120 |     // 测试桌面视图
  121 |     await page.setViewportSize({ width: 1200, height: 800 })
  122 |     await expect(page.locator('.document-table')).toBeVisible()
  123 | 
  124 |     // 测试移动端视图
  125 |     await page.setViewportSize({ width: 375, height: 667 })
  126 |     await expect(page.locator('.document-table')).toBeVisible()
  127 |   })
  128 | })
  129 | 
```