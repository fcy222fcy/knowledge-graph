# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: stats.spec.ts >> 分析统计页面测试 >> 趋势图表存在
- Location: tests\stats.spec.ts:28:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('.trend-chart')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('.trend-chart')

```

```yaml
- text: SE
- heading "欢迎回来" [level=1]
- paragraph: 登录 SE智图问答，继续你的学习之旅
- img
- textbox "请输入用户名"
- img
- textbox "请输入密码"
- checkbox "记住我" [checked]
- text: 记住我 忘记密码？
- button "登录"
- text: 还没有账号？ 立即注册
```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test'
  2   | 
  3   | test.describe('分析统计页面测试', () => {
  4   |   test.beforeEach(async ({ page }) => {
  5   |     // 设置 localStorage 模拟已登录状态
  6   |     await page.goto('/login')
  7   |     await page.evaluate(() => {
  8   |       localStorage.setItem('token', 'test-token')
  9   |       localStorage.setItem('userInfo', JSON.stringify({
  10  |         id: 1,
  11  |         username: 'testuser',
  12  |         nickname: '测试用户'
  13  |       }))
  14  |     })
  15  |     await page.goto('/stats')
  16  |   })
  17  | 
  18  |   test('页面加载正确', async ({ page }) => {
  19  |     // 检查页面标题
  20  |     await expect(page).toHaveTitle(/SE智图问答/)
  21  | 
  22  |     // 检查主布局存在
  23  |     await expect(page.locator('.app-layout')).toBeVisible()
  24  |     await expect(page.locator('.sidebar')).toBeVisible()
  25  |     await expect(page.locator('.main')).toBeVisible()
  26  |   })
  27  | 
  28  |   test('趋势图表存在', async ({ page }) => {
  29  |     // 等待页面加载
  30  |     await page.waitForLoadState('networkidle')
  31  | 
  32  |     // 检查趋势图表组件
> 33  |     await expect(page.locator('.trend-chart')).toBeVisible()
      |                                                ^ Error: expect(locator).toBeVisible() failed
  34  | 
  35  |     // 检查图表标题
  36  |     await expect(page.locator('text=学习趋势')).toBeVisible()
  37  |   })
  38  | 
  39  |   test('掌握度图表存在', async ({ page }) => {
  40  |     // 等待页面加载
  41  |     await page.waitForLoadState('networkidle')
  42  | 
  43  |     // 检查掌握度图表组件
  44  |     await expect(page.locator('.mastery-chart')).toBeVisible()
  45  | 
  46  |     // 检查图表标题
  47  |     await expect(page.locator('text=知识点掌握度')).toBeVisible()
  48  |   })
  49  | 
  50  |   test('热点排行存在', async ({ page }) => {
  51  |     // 等待页面加载
  52  |     await page.waitForLoadState('networkidle')
  53  | 
  54  |     // 检查热点排行组件
  55  |     await expect(page.locator('.hot-points-rank')).toBeVisible()
  56  | 
  57  |     // 检查排行标题
  58  |     await expect(page.locator('text=热点知识点排行')).toBeVisible()
  59  |   })
  60  | 
  61  |   test('薄弱点列表存在', async ({ page }) => {
  62  |     // 等待页面加载
  63  |     await page.waitForLoadState('networkidle')
  64  | 
  65  |     // 检查薄弱点列表组件
  66  |     await expect(page.locator('.weak-points-list')).toBeVisible()
  67  | 
  68  |     // 检查列表标题
  69  |     await expect(page.locator('text=薄弱知识点')).toBeVisible()
  70  |   })
  71  | 
  72  |   test('图表交互功能', async ({ page }) => {
  73  |     // 等待页面加载
  74  |     await page.waitForLoadState('networkidle')
  75  | 
  76  |     // 检查图表容器
  77  |     const chartContainers = page.locator('.chart-container')
  78  |     const count = await chartContainers.count()
  79  | 
  80  |     if (count > 0) {
  81  |       // 检查第一个图表
  82  |       await expect(chartContainers.first()).toBeVisible()
  83  |     }
  84  |   })
  85  | 
  86  |   test('时间范围选择', async ({ page }) => {
  87  |     // 等待页面加载
  88  |     await page.waitForLoadState('networkidle')
  89  | 
  90  |     // 检查时间范围选择器
  91  |     const timeRangeSelector = page.locator('.time-range-selector')
  92  |     if (await timeRangeSelector.isVisible()) {
  93  |       // 检查时间选项
  94  |       await expect(page.locator('text=最近7天')).toBeVisible()
  95  |       await expect(page.locator('text=最近30天')).toBeVisible()
  96  |     }
  97  |   })
  98  | 
  99  |   test('数据刷新功能', async ({ page }) => {
  100 |     // 等待页面加载
  101 |     await page.waitForLoadState('networkidle')
  102 | 
  103 |     // 检查刷新按钮
  104 |     const refreshButton = page.locator('button:has-text("刷新")')
  105 |     if (await refreshButton.isVisible()) {
  106 |       await refreshButton.click()
  107 | 
  108 |       // 检查加载状态
  109 |       // 这里需要根据实际实现来调整
  110 |     }
  111 |   })
  112 | 
  113 |   test('页面响应式布局', async ({ page }) => {
  114 |     // 测试桌面视图
  115 |     await page.setViewportSize({ width: 1200, height: 800 })
  116 |     await expect(page.locator('.trend-chart')).toBeVisible()
  117 |     await expect(page.locator('.mastery-chart')).toBeVisible()
  118 | 
  119 |     // 测试移动端视图
  120 |     await page.setViewportSize({ width: 375, height: 667 })
  121 |     await expect(page.locator('.trend-chart')).toBeVisible()
  122 |     await expect(page.locator('.mastery-chart')).toBeVisible()
  123 |   })
  124 | })
  125 | 
```