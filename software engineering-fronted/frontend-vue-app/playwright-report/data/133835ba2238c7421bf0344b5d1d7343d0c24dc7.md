# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: qa.spec.ts >> 问答中心测试 >> 新建会话功能
- Location: tests\qa.spec.ts:64:3

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: locator.click: Test timeout of 30000ms exceeded.
Call log:
  - waiting for locator('button:has-text("新建会话")')

```

# Page snapshot

```yaml
- generic [ref=e4]:
  - generic [ref=e5]:
    - generic [ref=e6]: SE
    - heading "欢迎回来" [level=1] [ref=e7]
    - paragraph [ref=e8]: 登录 SE智图问答，继续你的学习之旅
  - generic [ref=e9]:
    - generic [ref=e13]:
      - img [ref=e16]
      - textbox "请输入用户名" [ref=e18]
    - generic [ref=e22]:
      - img [ref=e25]
      - textbox "请输入密码" [ref=e28]
    - generic [ref=e31]:
      - generic [ref=e32] [cursor=pointer]:
        - generic [ref=e33]:
          - checkbox "记住我" [checked]
        - generic [ref=e35]: 记住我
      - generic [ref=e37] [cursor=pointer]: 忘记密码？
    - button "登录" [ref=e40] [cursor=pointer]:
      - generic [ref=e41]: 登录
  - generic [ref=e42]:
    - text: 还没有账号？
    - generic [ref=e44] [cursor=pointer]: 立即注册
```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test'
  2   | 
  3   | test.describe('问答中心测试', () => {
  4   |   test.beforeEach(async ({ page }) => {
  5   |     // 设置 localStorage 模拟已登录状态
  6   |     await page.goto('/login')
  7   |     await page.evaluate(() => {
  8   |       localStorage.setItem('token', 'test-token')
  9   |       localStorage.setItem('userInfo', JSON.stringify({
  10  |         id: 1,
  11  |         username: 'testuser',
  12  |         nickname: '测试用户'
  13  |       }))
  14  |     })
  15  |     await page.goto('/qa')
  16  |   })
  17  | 
  18  |   test('页面加载正确', async ({ page }) => {
  19  |     // 检查页面标题
  20  |     await expect(page).toHaveTitle(/SE智图问答/)
  21  | 
  22  |     // 检查主布局存在
  23  |     await expect(page.locator('.app-layout')).toBeVisible()
  24  |     await expect(page.locator('.sidebar')).toBeVisible()
  25  |     await expect(page.locator('.main')).toBeVisible()
  26  |   })
  27  | 
  28  |   test('会话列表存在', async ({ page }) => {
  29  |     // 等待页面加载
  30  |     await page.waitForLoadState('networkidle')
  31  | 
  32  |     // 检查会话列表存在
  33  |     await expect(page.locator('.session-list')).toBeVisible()
  34  | 
  35  |     // 检查新建会话按钮
  36  |     await expect(page.locator('button:has-text("新建会话")')).toBeVisible()
  37  |   })
  38  | 
  39  |   test('聊天面板存在', async ({ page }) => {
  40  |     // 等待页面加载
  41  |     await page.waitForLoadState('networkidle')
  42  | 
  43  |     // 检查聊天面板存在
  44  |     await expect(page.locator('.chat-panel')).toBeVisible()
  45  | 
  46  |     // 检查消息输入框
  47  |     await expect(page.locator('textarea[placeholder*="输入你的问题"]')).toBeVisible()
  48  | 
  49  |     // 检查发送按钮
  50  |     await expect(page.locator('button:has-text("发送")')).toBeVisible()
  51  |   })
  52  | 
  53  |   test('空状态显示', async ({ page }) => {
  54  |     // 等待页面加载
  55  |     await page.waitForLoadState('networkidle')
  56  | 
  57  |     // 如果没有会话，应该显示空状态
  58  |     const emptyState = page.locator('.empty-chat')
  59  |     if (await emptyState.isVisible()) {
  60  |       await expect(page.locator('text=开始新的问答')).toBeVisible()
  61  |     }
  62  |   })
  63  | 
  64  |   test('新建会话功能', async ({ page }) => {
  65  |     // 等待页面加载
  66  |     await page.waitForLoadState('networkidle')
  67  | 
  68  |     // 点击新建会话按钮
  69  |     const newSessionButton = page.locator('button:has-text("新建会话")')
> 70  |     await newSessionButton.click()
      |                            ^ Error: locator.click: Test timeout of 30000ms exceeded.
  71  | 
  72  |     // 检查新会话创建
  73  |     // 这里需要根据实际实现来调整
  74  |   })
  75  | 
  76  |   test('发送消息功能', async ({ page }) => {
  77  |     // 等待页面加载
  78  |     await page.waitForLoadState('networkidle')
  79  | 
  80  |     // 找到消息输入框
  81  |     const messageInput = page.locator('textarea[placeholder*="输入你的问题"]')
  82  |     await expect(messageInput).toBeVisible()
  83  | 
  84  |     // 输入消息
  85  |     await messageInput.fill('什么是软件工程？')
  86  | 
  87  |     // 点击发送按钮
  88  |     const sendButton = page.locator('button:has-text("发送")')
  89  |     await sendButton.click()
  90  | 
  91  |     // 检查消息是否发送
  92  |     // 这里需要根据实际实现来调整
  93  |   })
  94  | 
  95  |   test('会话切换功能', async ({ page }) => {
  96  |     // 等待页面加载
  97  |     await page.waitForLoadState('networkidle')
  98  | 
  99  |     // 检查会话列表
  100 |     const sessionItems = page.locator('.session-item')
  101 |     const count = await sessionItems.count()
  102 | 
  103 |     if (count > 1) {
  104 |       // 点击第二个会话
  105 |       await sessionItems.nth(1).click()
  106 | 
  107 |       // 检查聊天面板更新
  108 |       await expect(page.locator('.chat-panel')).toBeVisible()
  109 |     }
  110 |   })
  111 | 
  112 |   test('消息气泡显示', async ({ page }) => {
  113 |     // 等待页面加载
  114 |     await page.waitForLoadState('networkidle')
  115 | 
  116 |     // 检查消息气泡组件
  117 |     const messageBubbles = page.locator('.message-bubble')
  118 |     const count = await messageBubbles.count()
  119 | 
  120 |     if (count > 0) {
  121 |       // 检查第一个消息气泡
  122 |       await expect(messageBubbles.first()).toBeVisible()
  123 |     }
  124 |   })
  125 | 
  126 |   test('来源参考功能', async ({ page }) => {
  127 |     // 等待页面加载
  128 |     await page.waitForLoadState('networkidle')
  129 | 
  130 |     // 检查来源参考组件
  131 |     const sourceReferences = page.locator('.source-reference')
  132 |     const count = await sourceReferences.count()
  133 | 
  134 |     if (count > 0) {
  135 |       // 检查第一个来源参考
  136 |       await expect(sourceReferences.first()).toBeVisible()
  137 |     }
  138 |   })
  139 | 
  140 |   test('页面响应式布局', async ({ page }) => {
  141 |     // 测试桌面视图
  142 |     await page.setViewportSize({ width: 1200, height: 800 })
  143 |     await expect(page.locator('.chat-panel')).toBeVisible()
  144 | 
  145 |     // 测试移动端视图
  146 |     await page.setViewportSize({ width: 375, height: 667 })
  147 |     await expect(page.locator('.chat-panel')).toBeVisible()
  148 |   })
  149 | })
  150 | 
```